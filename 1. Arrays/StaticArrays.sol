pragma solidity ^0.8.30;
// SPDX-License-Identifier: UNLICENSED

contract StaticArrays {

    uint[10] static1; //static array with 10 elements
    // uint[10] static2 = new uint[10](); // now allowed for storage arrays

    // operations -> Same as dynamic arrays except for push and pop now allowed... 
}